@model Langtang.DataAccessLayer.ViewModel.PersonnalModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

<form id="formId">
    <div class="form-control">
        @Html.TextBoxFor(x => x.FIRSTNAME, new { @class = "input-form", required = "required" })
    </div>

    <input type="button" class="k-edit-buttons" id="submitData" />
</form>

<script>
    $("#submitData").click(function()
    {
        var form = $("form#formId");

        var firstName = $("#FIRSTNAME").val();
        var success = true;
        if(firstName.trim=="" || firstName.trim==null)
        {
            success = false;
        }
        if(success)
        {
            /*ajax{
            url:'',
            method:'POST',
            data:form.Serialize()
            .sucess(
            }
            */
        }

    })
</script>
